Database integration placeholder.

- PostgreSQL: SQL migrations in `backend/db-schemas/pg/*.sql`
- MongoDB: Mongoose schema models in `backend/db-schemas/mongo/*.schema.ts`

Next phase will add:
- ORM/ODM integration modules
- repositories/services
- migration runner configuration
