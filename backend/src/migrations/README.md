# PostgreSQL migrations

This folder will contain SQL migration files when you decide on a migration tool
(e.g., `node-pg-migrate`, `typeorm`, `knex`, or `sequelize`).

For now, generated SQL migration files are placed here:
- `001_*.sql` etc.

